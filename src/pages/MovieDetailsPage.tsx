import React from 'react';
import {MovieDetails} from "../components/MovieConteiner";

const MovieDetailsPage = () => {
    return (
        <div>
            <MovieDetails/>
        </div>
    );
};

export default MovieDetailsPage;